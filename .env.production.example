# Production environment variables (example)
#
# In Vercel you typically configure these via Project Settings → Environment Variables,
# not by committing a `.env.production` file.

NODE_ENV="production"

# Prefer a pooled/Prisma-optimized URL when your provider supports it.
# - Vercel Postgres provides a Prisma-compatible URL (recommended for Prisma)
# - Neon/Supabase can also work with pooled URLs; you may need `?pgbouncer=true`
#
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"

# DIRECT_URL is REQUIRED for Prisma migrations and introspection
# If using Supabase/Neon with connection pooling:
#   DATABASE_URL = pooled connection (for queries)
#   DIRECT_URL = direct connection (for migrations)
# If NOT using pooling, set DIRECT_URL to the same value as DATABASE_URL
DIRECT_URL="postgresql://USER:PASSWORD@HOST:5432/DB?sslmode=require"

JWT_SECRET="change-me-to-a-strong-secret"

# Vercel Cron (required for scheduled 48h follow-up breach notifications to admins).
# Add CRON_SECRET in Vercel dashboard (16+ random characters). Vercel attaches it as Bearer token on cron requests.
# CRON_SECRET="your-production-cron-secret"

# Vercel Hobby: at most one cron invocation per day (see vercel.json schedule).
# Pro/Enterprise: you may change vercel.json crons to e.g. "0 * * * *" for hourly checks.

# Optional: AI Chat (Gemini)
# GEMINI_API_KEY="your-gemini-api-key"

# Optional: S3 storage
# AWS_REGION="us-east-1"
# AWS_ACCESS_KEY_ID="..."
# AWS_SECRET_ACCESS_KEY="..."
# AWS_S3_BUCKET_NAME="..."
